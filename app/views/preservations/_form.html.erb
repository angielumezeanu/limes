<%= form_with(model: preservation, local: true) do |form| %>
  <% if preservation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(preservation.errors.count, "error") %> prohibited this record from being saved:</h2>

      <ul>
      <% preservation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Preservation Status<br/>
    <%= form.text_field :name, id: :preservation_name %>
  </div>

  <div class="actions">
    <%= form.submit "Save"%>
  </div>
<% end %>
