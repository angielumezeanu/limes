<%= form_with(model: intact, local: true) do |form| %>
  <% if intact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intact.errors.count, "error") %> prohibited this intact from being saved:</h2>

      <ul>
      <% intact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :degree %>
    <%= form.text_field :degree, id: :intact_degree %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :intact_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
